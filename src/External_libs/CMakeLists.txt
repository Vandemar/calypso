# Make the FFTPACK5.1D library
ADD_LIBRARY(fftpack.5d FFTPACK5.1D/fftpack5.1d.f90)

IF (CUDA_FOUND AND CMAKE_C_COMPILER_WORKS)
  ADD_DEPENDENCIES(fftpack.5d iFortran)
ENDIF (CUDA_FOUND AND CMAKE_C_COMPILER_WORKS)
